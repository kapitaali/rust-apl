# Progress — 2026-09-05

> **Context-preservation file.**

---

## Session 110-115 (2026-09-05) — SQL Fix + Data Exchange Plugins + Phase 9

### What was implemented

**SQL plugin fix:**
- `SqlPlugin` struct + `AplPlugin` impl (was missing)
- `quad_sql` uses `cells()` + `Cell::Char(ch)` matching
- rusqlite `bundled` feature for no-system-lib builds

**All data exchange plugins enabled:**
- `fft.rs` — FftPlugin added, Cell import, ShapeItem path fixed
- `cdr.rs` — CdrPlugin added, ValueP::Char pattern fixed
- All plugins registered via config.toml

**Plugin middleware hooks:**
- `before_eval(&Expr)` — blocks expressions
- `before_syscmd(&str)` — blocks system commands
- `on_sysvar_change(&str, &ValueP)` — notifies on ⎕-var changes

**⎕SEC Security Extension:**
- SecurityPlugin registers ⎕SEC sysvar (default 0)
- Level 1: blocks ⍎, ⎕NA, ⎕LOADSO, )COPY, )INP
- Level 2: additionally blocks )SAVE, )LOAD, )OUT, )CONTINUE, ⎕FIO

**ASCII Plotting Expansion:**
- Bar charts for vectors, line plots for matrices
- Legend for multiple series
- `render_bar_chart`, `render_line_plot`, `lines_to_matrix`

**pyo3 Integration:**
- In-process Python execution when `plugin-python` feature enabled
- Falls back to shell-out when pyo3 not available

**⎕FIO Complete:**
- All 10 functions (0-9) work correctly
- Write line (4) and write bytes (6) now functional
- File stored directly (not BufReader) for read+write

**⎕SVx Shared Variables:**
- In-process registry with offer/query/read/set/cancel
- ⎕SVC lists, ⎕SVO offers, ⎕SVQ queries, ⎕SVR reads, ⎕SVS sets

### Roadmap Status

All Phase 9 features are now complete. The rewrite covers:

| Phase | Status |
|---|---|
| 1 — Quad Functions | ✅ Done |
| 2 — Operators | ✅ Done |
| 3 — Selective Assignment | ✅ Done |
| 4 — Native Functions | ✅ Done |
| 5 — Infrastructure | ✅ Done |
| 6 — Plugin System | ✅ Done |
| 7 — Performance | ✅ Done |
| 8 — XML Compatibility | ✅ Done |
| 9 — Remaining Features | ✅ Done |

### Verification
- Tests: 777 passing
- Build: clean (debug + release)
- No errors, no warnings (19 pre-existing dead-code warnings)
