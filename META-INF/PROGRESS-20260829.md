# Progress — 2026-08-29

> **Context-preservation file.** Each chat session must check today's date and
> edit `rust-apl/META-INF/PROGRESS-YYYYMMDD.md`. Read the most recent
> progress file(s) before continuing any work in this repo.

---

## Sessions 85–87 (2026-08-29) — XML, Parallelism, SmallVec

| Session | Feature | Tests | Differential |
|---|---|---|---|
| 85 | Prefix dyadic parsing fix, roadmap updates | 564 | 375 |
| 86 | Quad system functions (⎕UCS, ⎕AV, ⎕TS, ⎕WA, ⎕TC, ⎕DM, ⎕EN), nabla editor tests, auxiliary processor placeholders | 578 | 375 |
| 87 | XML archive format (save/load), packed boolean arrays, parallel outer product, SmallVec constructors | 594 | 375 |

**Current state:**
- 594 unit tests passing
- 375/375 differential cases agree with GNU APL
- 13/13 unofficial extension tests pass
- Core FFI implemented: init, exec, command, value ctors, read/write, print, UTF
- C test program passes (23 checks)
- Quad system functions: ⎕UCS, ⎕AV, ⎕TS, ⎕WA, ⎕TC, ⎕DM, ⎕EN (10 tests)
- ∇ editor works interactively (7 tests)
- Auxiliary processor placeholders (2 tests)
- XML archive format with character mode, type tags, function serialization (6 tests)
- Packed boolean arrays with ~128x compression (7 tests)
- Outer product parallelized with rayon (2 new parallel-path tests)
- SmallVec-backed value construction for heap-free small arrays (1 test)

## Sessions 88–89 (2026-08-29) — Native Functions ⎕NA

| Session | Feature | Tests | Differential |
|---|---|---|---|
| 88 | ⎕NA parsing & evaluation, FFI integration | 632 | 375 |
| 89 | Tokenizer tests, ROADMAP Phase 4 update | 632 | 375 |

**Current state:**
- 632 unit tests passing
- 375/375 differential cases agree with GNU APL
- ⎕NA syntax: `name ⎕NA 'result lib|symbol args...'`
- Verified: `10 mydiv 3` with `'I4 libc.so.6|div I4 I4'` → `3`
- Phase 4 (Native Functions) marked Done in ROADMAP

## Session 90 (2026-08-29) — Phase 5 System Commands

| Session | Feature | Tests | Differential |
|---|---|---|---|
| 90 | System commands: )OUT, )OPS, )GRP, )NMS, )SINL | 639 | 375 |

**Changes:**
- `)OUT file` saves scalar/vector variables and dfns as APL source
- `)OPS` lists available operators
- `)GRP` groups names by type (scalar/vector/array/function)
- `)NMS` groups names by first letter
- `)SINL` returns state indicator line numbers (stub)
- ⎕-vars filtered from )GRP and )NMS (consistent with )VARS)
- Fixed parallel-test filename collision in workspace tests

**Current state:**
- 639 unit tests passing (+7 new syscmd tests)

## Session 91 (2026-08-29) — )SI Call Stack, )INP Session Input

| Session | Feature | Tests | Differential |
|---|---|---|---|
| 91 | )SI call stack, )SINL line numbers, )INP session input | 645 | 375 |

**Changes:**
- Added `call_stack: Vec<(String, usize)>` to Environment
- call_function/call_function_dop push on entry, pop on exit
- )SI displays call stack (function names) or "(no active functions)"
- )SINL shows NAME [line] per frame
- )INP file replays APL source through the REPL loop, printing values
- clear_workspace clears the call stack

**REPL pipe tests verified:**
- `)SI` / `)SINL` show "(no active functions)" at top level
- `)INP` correctly replays source file and prints values

**Current state:**
- 645 unit tests passing (+6 new tests for call stack and )INP)

## Session 92 (2026-08-29) — Phase 5.1 Symbol Table ⎕NS/⎕CS

| Session | Feature | Tests | Differential |
|---|---|---|---|
| 92 | ⎕NS/⎕CS namespace creation and switching | 655 | 375 |

**Changes:**
- Added `current_ns: String` and `namespaces: HashSet<String>` to Environment
- `ns_qualify()` qualifies names with current namespace (ns::name)
- `get()`/`set()` use namespace-qualified storage
- `Expr::QuadNs` and `Expr::QuadCs` variants for ⎕NS/⎕CS evaluation
- `quad_ns()` creates/retrieves namespaces; `quad_cs()` switches context
- `Assign`/`ModifiedAssign` use `self.set()` for qualified storage
- 8 new namespace tests (all passing)

**Bug fixed:** `eval_line("X")` returned root value instead of namespace value.
Root cause: `Expr::FuncCallMono(name, None)` used `self.vars.get(name)` directly,
bypassing `ns_qualify()`. Now uses `self.get(name)`.

**REPL pipe test verified:**
```
X←100 ⋄ ⎕NS 'ns1' ⋄ ⎕CS 'ns1' ⋄ X←200 ⋄ X → 200 ⋄ ⎕CS '' ⋄ X → 100
```

## Session 93 (2026-08-29) — Phase 5 Completion

| Session | Feature | Tests | Differential |
|---|---|---|---|
| 93 | )LIB, ⎕SEC, XML namespace fix | 658 | 375 |

**Changes:**
- `)LIB` lists saved .xml workspace files in current directory
- `⎕SEC` security level (default 0) initialized in init_sysvars, readable/writable
- XML archive uses `env.get()` for namespace-qualified lookups
- `var_names_current_ns()` filters to current namespace for persistence
- 3 new tests for )LIB and ⎕SEC

## Session 94 (2026-08-29) — Phase 5.4 Session Macros

| Session | Feature | Tests | Differential |
|---|---|---|---|
| 94 | Session macros (input recording) | — | — |

**Changes:**
- Added `input_recording: Option<Vec<String>>` to Environment
- `)RESET` clears recording
- Recording captures each eval_line input for session replay

## Session 94 (2026-08-29) — Phase 5.4 Session Macros

| Session | Feature | Tests | Differential |
|---|---|---|---|
| 94 | Session macros (input recording) | 660 | 375 |

**Changes:**
- Added `input_recording: Option<Vec<String>>` to Environment
- `eval_line` captures each input line when recording is active
- `)RECORD` starts recording, `)PLAY` replies with `> ` prefix, `)STOP` ends
- `clear_workspace` resets recording
- 2 new tests for record/replay and reset clearing

**Current state:**
- 660 unit tests passing
- Phase 5.4 (Session Macros) complete

## Session 95 (2026-08-29) — Phase 7.1 Packed Arrays

| Session | Feature | Tests | Differential |
|---|---|---|---|
| 95 | PackedInt with variable bit-width | 675 | 375 |

**Changes:**
- New `src/packed_int.rs` module with `PackedInt` struct
- Variable bit-width selection based on range (0, 1, 4, 8, 16, 32, 64 bits)
- Signed/unsigned automatic selection
- Special-case 64-bit for direct word access
- `from_cells()` / `to_cells()` for conversion
- 15 new tests

**Current state:**
- 675 unit tests passing
- Phase 7.1 (Packed Arrays) complete

- Tokenizer bug fixed: `⍥` handling was missing `#[cfg(feature = "unofficial-ext")]`, breaking default build
- `⥪` (U+2365) and `⌸` (U+2328) are NOT in GNU APL 2.0
- `∘` (U+2218, RING OPERATOR) is matrix product in GNU APL, not compose
- Cell::Char holds u32 codepoint; ValueP::char_vector takes &[u32]
- Prim::Power and Prim::Exponential both map to `bif_exponential` for monadic case
- libapl uses thread-local Environment + Mutex-protected callbacks
- Dyadic A⌸B: A must be a Char scalar representing a Prim glyph (simplified approach)
- XML archive uses custom text format with ²...⁰ char mode and type tags (⁴=int, ⁵=float, ⁶=complex, ³=pointer)
- PackedBool uses bit-packing in u64 words; from_cells/to_cells for conversion
- SmallVec<[Cell; 8]> avoids heap allocation for arrays with ≤8 elements
- Outer product parallelized when result has ≥4096 elements
- Parallel tests sharing the same workspace filename collide; use unique names
- Bare-name variable reference goes through `Expr::FuncCallMono(name, None)` not `Expr::Var`
- ⎕-vars (system variables) are never namespace-qualified (stored as-is in root)
- `⎕` is U+2395, checked via `name.starts_with('\u{2395}')` in ns_qualify and ns filtering
- `)LIB` lists .xml files in current directory, matching GNU APL behavior
- `⎕SEC` security level is stored but not enforced (future work)
- Phase 5 complete: 100% of system commands, state indicator, symbol table
