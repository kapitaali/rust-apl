# Progress — 2026-08-28

> **Context-preservation file.** Each chat session must check today's date and
> edit `rust-apl/META-INF/PROGRESS-YYYYMMDD.md`. Read the most recent
> progress file(s) before continuing any work in this repo.

---

## Sessions 79–81 (2026-08-28) — Unofficial extensions

| Session | Feature | Tests | Differential |
|---|---|---|---|
| 79 | Complex power fix, depth-of-char-scalar fix | 564 | 374→375 |
| 80 | Unofficial ext infrastructure, ⌸ key, ⍥ over | 564 | 375 |
| 81 | ⍥ dyadic over, README docs, 13 unofficial tests | 564 | 375 |

**End of day 2026-08-28:**
- 564 tests, 375/375 differential agree
- 13/13 unofficial extension tests pass
- All commits pushed to origin/main
- Release build clean

## Current state (2026-08-28)

- **564 unit tests passing**
- **375/375 differential cases agree** with C++ GNU APL
- **13/13 unofficial extension tests pass** (⌸ key, ⍥ over)
- Clippy clean, Release build clean
- Parallel dispatch active for 19 operations
- AplError with source-line display in REPL
- System commands: `)SAVE`, `)LOAD`, `)COPY`, `)DROP`, `)CLEAR`, `)FNS`, `)VARS`, `)LIB`, `)OFF`, `)ERASE`, `)DIGITS`, `)WIDTH`, `)CONTINUE`, `)DIR`, `)HISTORY`, `)SI`, `)SYMBOLS`, `)UCS`
- Last commit: `482d57c` (README docs for unofficial extensions)

## Remaining roadmap

1. **libapl C API** — Major embedding API (~400 lines: init/exec/repl/value-ctors/function-eval/callbacks). Requires dedicated design session. Not in GNU APL reference.
2. **Tokenizer spans for caret display** — Complex refactor, low ROI without libapl-style embedding

## Non-obvious notes

- ⎕AV is the APL character vector (0-255); `⎕AV[⎕AV⍳'A']` returns the character
- `⍥` (U+2365, "APL FUNCTIONAL SYMBOL OVER") is NOT in GNU APL 2.0
- `⌸` (U+2328, "APL FUNCTIONAL SYMBOL QUAD") is NOT in GNU APL 2.0
- `∘` (U+2218, RING OPERATOR) is matrix product in GNU APL, not compose
- GNU APL's `)COPY`, `)CS`, `)DROP`, `)IN`, `)LOAD`, `)OUT`, `)PROP`, `)SAVE`, `)SI`, `)SYMBOLS`, `)TID`, `)TS`, `)UL`, `)UCS`, `)XOFF`, `)XON`, `)WIDTH`, `)DIGITS` are all session-management commands
- `⎕BOXING` value 1 = boxed display (default), 0 = plain
- Cell::Char holds u32 codepoint; ValueP::char_vector takes &[u32]
- Prim::Power and Prim::Exponential both map to `bif_exponential` for monadic case
- `1⎕CR` produces flat ravel with parens for nested elements
- `⋆` (U+22C6) and `*` (ASCII) both tokenize to Prim::Power; monadic * goes through Power→bif_exponential
