# Progress — 2026-08-22

> **Context-preservation file.** Each chat session must check today's date and
> edit `rust-apl/META-INF/PROGRESS-YYYYMMDD.md` (create it in that directory
> if missing). Read the most recent progress file(s) before continuing any
> work in this repo.
>
> **Note:** progress files live in `rust-apl/META-INF/` (moved there on
> 2026-08-22; originally at repo root, then briefly at repo-root META-INF/).
> Older references to `PROGRESS-YYYYMMDD.md` at the repo root mean this directory.

---

## Project state (end of this session)

**Repo:** `/home/theb/Apps/apl-2.0` — GNU APL 2.0 (C++ original, untouched)

### Key documents

| File | What it is |
|---|---|
| `/home/theb/Apps/apl-2.0/API.md` | Comprehensive API reference of the C++ source: 403 classes, 53 structs, 84 enums, 71 typedefs across 24 sections. Generated via `.api-docs/extract_api.py` + `.api-docs/build_api_md.py`; raw inventory at `.api-docs/INVENTORY.md`. |
| `/home/theb/Apps/apl-2.0/REWRITE_STRATEGY.md` | Strategy for rewriting GNU APL in ANSI C vs Rust. **Verdict: Rust.** Contains phased migration plan (Phases 0–10) and appendix with target Rust type definitions. |
| `/home/theb/Apps/apl-2.0/.api-docs/parts/*.md` | Pre-existing partial docs (18 files, 7600 lines) that predate this work — good deep-dive material on specific subsystems. |
| `/home/theb/Apps/apl-2.0/rust-apl/README.md` | Status table of the Rust port + build instructions. |

### The Rust port — `/home/theb/Apps/apl-2.0/rust-apl/`

**Status: working REPL, 25 tests passing, clean clippy, release build verified.**

| File | Mirrors (C++) | Contents |
|---|---|---|
| `Cargo.toml` | — | single dep: smallvec; release = LTO |
| `src/types.rs` | `APL_types.hh`, `APL_enums.hh`, `Error.def` | typedefs (`ShapeItem=i64`, `APLInteger=i64`, `Unicode=u32`, `APLFloat=f64`, `APLComplex`), `CellType` bitflags, `ErrorCode` enum + `AplResult<T>` alias, `MAX_RANK=8` |
| `src/cell.rs` | `Cell*.hh/cc` | `enum Cell { Char, Int, Float, Complex, Pointer, Lval }`, type tests, tolerant equality (⎕CT), ordering (Pointer > Numeric > Char), all scalar `bif_*` ops (+ − × ÷ ! ⌈ ⌊ ∣ ○ etc.), Lanczos tgamma, binomial |
| `src/shape.rs` | `Shape.hh/cc` | rank/rho/volume struct, constructors (scalar/vector/matrix/cube/from_dims), accessors, mutators, shape catenation via `Add` |
| `src/value.rs` | `Value.hh`, `Value_P.hh` | `ValueP` = `Rc<ValueInner>` with COW `isolate()`/`make_mut()`; constructors incl. `iota()`, char/int vectors; `from_parts`, `from_ravel_like`; Debug formatting |
| `src/functions.rs` | `ScalarFunction.cc`, parts of `Bif_F12_*` | `Prim` enum (symbol→function dispatch), monadic/dyadic eval, elementwise broadcast with scalar extension, reshape `A⍴B`, monadic shape `⍴B` and iota `⍳B` |
| `src/tokenizer.rs` | `Tokenizer.cc` (simplified) | numbers (incl. `¯` negative, exponents), names (letters/digits/`_`/`∆`/`⍙`), strings with `''` escape, primitive glyphs, comments `⍝` |
| `src/parser.rs` | `Parser.cc` + prefix machine (simplified) | recursive descent, **right-to-left evaluation**, parentheses, assignment `←`, numeric strands (`2 3⍴...`), `Environment` (HashMap bindings); 10 parser-level tests locking APL semantics |
| `src/main.rs` | `main.cc` (minimal) | REPL: prompt, eval, formatted output (vectors one-line, matrices multi-line), `)OFF` to quit |

**Verified semantics (test-locked):**
- `2×3+4 = 14` (right-to-left), `5-1-2 = 6`
- `(2×3)+4 = 10` (parentheses override)
- Scalar extension over vectors and matrices
- Assignment binds silently, later expressions see the binding
- Reshape `M←2 3⍴⍳6` → rank 2, correct ravel
- Clean syntax errors on malformed input

### Session history (this date)

1. Got acquainted with repo (GNU APL 2.0, autotools C++, ~260 src files).
2. Wrote `.api-docs/extract_api.py` → inventory (403 classes etc.) → synthesized `API.md` (24 sections).
3. Wrote `REWRITE_STRATEGY.md` (C vs Rust analysis, verdict: Rust, 10-phase plan).
4. Started Rust implementation per plan Phases 1–4 (partial): types/cell/shape/value/functions/tokenizer/parser/REPL.
5. Fixed: strand parsing (`2 3⍴`), reduce-not-yet test, matrix display in REPL, clippy warnings.
6. **Reduce `/` + Scan `\`** (`src/operators.rs`): right-to-left fold (`-/1 2 3=2`, `÷/6 3 2=4`),
   empty-axis identity (`+/⍳0→0`, `×/⍳0→1`, `-/⍳0` → DOMAIN ERROR), row-wise on matrices.
   Scan = prefix-reduce with correct non-commutative direction (`-\1 2 3 = 1 ¯1 2`).
7. Parser fix: operators bind tighter than functions — `×/20⍴2` parses as `×/(20⍴2)` (was panicking);
   tokenizer handles bare `/` `\` after a primitive LO.
8. **Take `↑` / Drop `↓`** (`src/take_drop.rs`): over-take pads with prototype (0 / space),
   negative counts from the end, front-padding for negative over-take, per-row on matrices.
   Wired into `Prim::Take/Drop`, tokenizer `PRIM_SYMBOLS`, dyadic dispatch.
9. Final state: **38 tests passing**, clean clippy, release build verified.
10. **Rotate `A⌽B` / Reverse `⌽B`** (`src/rotate.rs`): positive shift left, negative right,
    per-row shifts from vector A, `⌽B` reverses. Ported from `Bif_ROTATE::rotate/reverse`.
11. **Grade up/down `⍋⍒`** (`src/sort.rs`): stable index sort (ties keep left-to-right order),
    returns permutation vectors.
12. **Bracket indexing `B[expr]`**: tokenizer `[` `]` tokens, parser `Expr::Index`,
    ravel indexing with INDEX ERROR on out-of-range. Enables the idiomatic
    `B[⍋B]` sort pattern — verified end-to-end in the REPL.
13. Final state: **54 tests passing**, zero clippy warnings, release build verified.
14. **Dyadic `⍳` (index-of)** (`src/index_of.rs`): first-occurrence index per B element,
    not-found → len(A), result shape follows B, tolerant equality via ⎕CT,
    empty-A edge case. Ported from `Bif_F12_INDEX_OF.cc` (linear path; the
    binary-search optimization for large A is deferred).
15. **Membership `A∈B`** (`src/epsilon.rs`): boolean ravel with A's shape.
16. Final state: **66 tests passing**, zero clippy warnings, release build verified.
17. **Transpose `⍉B`** (`src/transpose.rs`): matrix transpose; rank ≥ 3 swaps axes 0 and 1
    via stride-based index mapping (bug fixed: output counter must iterate OUT dims,
    not source dims). Vectors/scalars unchanged.
18. **Commute `A F⍨ B = B F A`** (`Tok::Commute`, `Expr` handled in `parse_simple`):
    argument swap; stray `⍨` is a clean SYNTAX ERROR.
19. Final state: **73 tests passing**, zero clippy warnings, release build verified.
20. **Selective assignment `B[i]←x`** (`Expr::AssignIndexed`): parser recognizes
    `NAME[expr] ← expr`, eval mutates the variable in place with COW isolation
    (`ValueInner::ravel_mut()` added). Index-vector form works (`V[1 3]←7`),
    INDEX ERROR on out-of-range, and a dedicated test proves shared values
    are never corrupted (A unchanged after B[0]←99 when B←A).
21. Final state: **77 tests passing**, zero clippy warnings, release build verified.
22. **Dyadic transpose `A⍉B`** (`transpose_dyadic`): full axis permutation — A[k] says
    which axis of B becomes result axis k. Validates: RANK ERROR on wrong perm
    length or out-of-range axis, DOMAIN ERROR on repeated axes (merged-axes
    case unsupported, documented). Identity perm and swap verified equal to
    monadic transpose semantics; 6 new tests.
23. Final state: **83 tests passing**, zero clippy warnings, release build verified.
24. **Nested arrays — core infrastructure**: `PointerCellData` redesigned from a
    handle-into-arena to a direct `Rc<ValueInner>` (Rust's type system replaces
    the arena; `Rc::strong_count` plays owner_count). Added:
    - `Cell::get_nested_value()`, `ValueP::nested()` (enclose), `ValueP::disclose()`
      (pick-through-pointer-scalar)
    - Pointer equality = identity (`Rc::ptr_eq`) OR deep ravel+shape comparison
    - `ValueInner::cells()` / `.shape()` public accessors
    - Monadic `↑B` discloses pointer scalars; REPL prints nested ravels
25. Final state: **86 tests passing**, zero clippy warnings, release build verified.
26. **Enclose `⊂B` / disclose `⊃B`** (`src/enclose.rs`): first-class primitives on the
    existing nesting infrastructure. ⊂ wraps into a pointer scalar (shares data via
    Rc); ⊃ unwraps pointer scalars, identity for simple scalars and non-scalars
    (full mixed disclosure deferred). Dyadic ⊂ (partition) and ⊃ (pick) not yet.
    6 tests incl. roundtrip, sharing, nested-matrix disclose.
27. Final state: **92 tests passing**, zero clippy warnings, release build verified.
28. **Each `¨` operator** (`Tok::Each`, `Expr::EachOp`, `operators::each`):
    tokenizer converts `F` + `¨` into `Tok::Each(F)`; parser binds the whole
    expression to its right (operator precedence, like reduce/scan); each
    ravel element is evaluated monadically as a scalar and the result nested
    via PointerCell. Result keeps B's shape. Dyadic Each not yet. 2 tests
    (values verified through pointer disclosure; stray ¨ = SYNTAX ERROR).
29. Final state: **94 tests passing**, zero clippy warnings, release build verified.
30. **Nested literals** `(1 2)(3 4 5)`: `Expr::NestedVec`; parser treats adjacent
    paren groups (optionally mixed with bare atoms, e.g. `(1) 2`) as a strand
    whose items are each enclosed. Elements may be arbitrary expressions
    (`(2+3)(⍳4)` works). Eval encloses each item via ValueP::nested().
    ValueInner gained element_count(). 3 tests.
31. Final state: **97 tests passing**, zero clippy warnings, release build verified.
32. **Dyadic pick `A⊃B`** (`src/pick.rs`): semantics taken from Pick.tc — each A
    element addresses ONE level of B. Scalar element → vector level (RANK ERROR
    on higher-rank level, INDEX ERROR out of range); enclosed element (⊂1 0) →
    full coordinate tuple for a multi-dimensional level (row-major weights).
    Pointers disclosed along the path AND at the end (simple cells wrapped as
    scalars). Empty A returns B; A rank > 1 → RANK ERROR. Verified against
    Pick.tc cases: `2 4⊃Q`, `(⊂2 2)⊃C` pattern, `(1 3) 2⊃S` (0-based).
    7 tests.
33. Final state: **103 tests passing**, zero clippy warnings, release build verified.
34. **Enlist `∊B`** (`src/enlist.rs`): recursive flatten to a rank-1 vector of
    simple scalars. Pointer cells recurse; empty values use their prototype
    (numeric → 0, char → space); deeply-nested scalars unwrap (⊂⊂⊂5 → ,5).
    **Prototype support added to ValueInner** (`proto` field, `new_with_proto`,
    `proto()` accessor; `char_vector` sets Char(' ') proto so ∊'' → ,' ').
    Tokenizer: `∊` (U+220A SMALL ELEMENT OF) added as alias of `∈` (GNU APL
    uses the small glyph). 8 tests incl. mixed-type flatten and empty cases.
35. Final state: **111 tests passing**, zero clippy warnings, release build verified.
36. **Mixed arrays** (`1 'a' 2`): parse_strand generalized — gathers Num AND Str
    literals; all-number strands stay flat NumVec, mixed strands become
    NestedVec with each item enclosed. Verified `2 2⍴1 'a' 2 'b'` reshape +
    display + pick. REPL nested display improved (format_nested recurses,
    shows bare scalars instead of Debug dumps). 3 tests.
37. Final state: **114 tests passing**, zero clippy warnings, release build verified.
38. **Prototype-based over-take padding** (`take_drop.rs::prototype()`): now reads
    `ValueInner.proto()` instead of inferring from the first cell — so padding
    works even for EMPTY values (`3↑'' → three spaces`, was broken before).
    Floats pad with 0.0; nested prototypes recurse (enclosed inner prototype).
    3 tests.
39. Final state: **117 tests passing**, zero clippy warnings, release build verified.
40. **Dyadic each `A F¨ B`** (`Expr::EachDyad`, `operators::each_dyad`): pairs
    corresponding elements of A and B (scalar extension allowed, LENGTH ERROR
    otherwise), applies dyadic F via the shared apply_prim dispatcher, nests
    each result. Parser: bare `Tok::Each` after a lhs in parse_simple means
    dyadic each (tokenizer merges F+¨ into Each(F), so the earlier
    (Prim,Each) pair check never fired — caught by tests). 3 tests.
41. Final state: **120 tests passing**, zero clippy warnings, release build verified.
42. **Selective pick assignment `(A⊃B)←x`** (`Expr::AssignPick`,
    `try_parse_pick_target`, `pick.rs::pick_assign`): parser recognizes
    `(index ⊃ NAME) ← expr` at line start; recursive pick_assign walks the
    path isolating (COW) each nested level, then replaces the final cell.
    2 bugs found & fixed during development: (a) branch was nested inside
    the Name-check block so lines starting with LParen never reached it;
    (b) try_parse_pick_target depth accounting was off by one relative to
    the opening paren. COW safety proven by test (A unchanged after
    (0 0⊃B)←77 when B←A). 2 tests.
43. Final state: **122 tests passing**, zero clippy warnings, release build verified.
44. **First-axis operators `⌿` / `⍀`** (`reduce_first`, `scan_first`): column-wise
    reduce/scan on rank ≥ 2 (vectors delegate to last-axis versions). Empty
    first axis → identity fill per remaining cell (⎕IO-style), DOMAIN ERROR
    without identity. Two bugs caught by tests: (a) scan output must be
    interleaved row-major, not column-blocks; (b) empty-matrix identity needs
    one cell per remaining-axis element. 5 tests.
45. Final state: **127 tests passing**, zero clippy warnings, release build verified.
46. **Depth `≡B` / Match `A≡B`** (`src/depth.rs`, mirrors `Bif_F12_EQUIV` +
    `Value::compute_depth()`): monadic depth — simple scalar 0, non-scalar
    1 + max sub-depth (pointer recursion); dyadic match — shape equality +
    deep cell equality through pointers, ⎕CT-tolerant. Both wired to the
    `≡` glyph (monadic/dyadic dispatch). 8 tests.
47. Final state: **135 tests passing**, zero clippy warnings, release build verified.
48. **Defined functions (∇) — IN PROGRESS, ~90% done.** See "ACTIVE WORK" below.
49. Final state: **145 tests passing**, zero clippy warnings.

### ACTIVE WORK — defined functions (resume here next session!)

What EXISTS and works (all tested):
- `src/functions_def.rs`: `DefinedFunction` struct, `FunctionTable`, `parse_header`
  (`Z←NAME X Y` / `DOUBLE X` / `HELLO` forms), `define_function(table, header,
  &[body_lines])`. 5 tests in-module.
- `Environment.funcs: FunctionTable`; `Environment::call_function(name, left,
  right)` with dynamic-scoping shadow save/restore (args + result var + body-
  assigned locals via `collect_assigned_names`), arity check, explicit result
  var captured BEFORE shadow restore.
- Parser: `Expr::FuncCallMono(name, Option<Box<Expr>>)` (bare name or `FN value`;
  only fires when next token starts a VALUE — Num/Str/Name/LParen — else it's a
  variable + prim like `X+1`) and `Expr::FuncCallDyad` (`A FN B`) in parse_simple.
- Eval: FuncCallMono falls back to variable read when name not in func table;
  FuncCallDyad always calls. 6 parser tests (monadic call, dyadic call,
  local shadowing restored, ambivalent call, branch test).
- **Branch arrow `→`**: `Prim::Branch`, tokenizer glyph, whole-right-expression
  operand (like operators). Eval records target in
  `Environment.last_branch_target` (empty target = no jump; 0 = exit; N = jump
  to line N 1-based). `call_function` body loop is now a `while pc < len`
  loop consuming `last_branch_target.take()`.
- REPL ∇ mode: `∇HEADER` starts definition, lines accumulate with `[n]`
  prompts, lone `∇` compiles+installs ("FAC defined"). Char-boundary-safe
  slicing (use `.chars().skip(1)` — ∇ is 3 bytes).
- Also added this session: comparison prims `≤ < = ≥ > ≠` (elementwise 0/1
  via Cell::compare Lt/Eq/Gt — NO Result wrapper), monadic `~B` not (1−B),
  `*` power glyph, `?` roll glyph. Note: `"~"` ASCII = Not; `"∼"` U+223C =
  Roll alias; `"?"` = roll.

**THE ONE REMAINING BUG** (was debugging when stopped):
Recursive FAC still broken. Debug findings:
- Non-recursive branch functions work: `→2` unconditional jump OK,
  `→0` exit OK, base-case-only function returns correctly.
- `FAC 5` fails SYNTAX ERROR; direct `call_function("FAC", ..., Int(0))`
  stack-overflows. Trace for N=1: `(N≤1)×2 = 2` → branch to line 2 =
  the RECURSION line → infinite descent (my test's APL was wrong: →2 jumps
  TO line 2, doesn't skip it).
- Correct FAC layout to use (verified pieces individually, just reassemble):
  ```
  ∇R←FAC N
  →(N≤1)×4        ⍝ base: jump to line 4 (R←1)
  R←N×FAC N-1     ⍝ recursive case
  →0              ⍝ exit after recursion
  R←1             ⍝ base case result
  ∇
  ```
- Last debug run of THIS exact layout gave `FAC 0/1 = 1 ✓, FAC 5 = SYNTAX
  ERROR`. Suspect: after recursion returns into `R←N×FAC N-1`, the inner
  call's branch state or shadow-restore interferes, OR the `→0` line exits
  before R is read... check whether explicit-result capture happens AFTER
  the loop breaks on →0 (it should — capture is after the while loop) and
  whether the INNER recursive call's `last_branch_target` leaks into the
  OUTER frame's loop (likely bug! inner →0 sets last_branch_target=Some(0),
  outer loop then sees it via take() and breaks early — but R was already
  set... actually the SYNTAX ERROR suggests explicit_result=None AND
  last=None somewhere). NEXT STEP: write a debug example tracing FAC 2
  line-by-line (print pc transitions + last_branch_target at each step);
  likely fix = save/clear last_branch_target around the recursive
  self.eval() call, or make branch targets per-call-frame (a Vec stack).

After fixing: add the FAC 5 test to parser.rs tests, run full
cargo fmt/test/clippy, REPL end-to-end test (script in /tmp/tf.txt pattern),
update items 48–49, move Defined Functions to done in roadmap.

### Conventions established this session

- Subagents: **max 2 at a time** (rate limits) — prefer doing work directly.
- Check date → edit/create `PROGRESS-YYYYMMDD.md` at session start when resuming.
- Rust code lives in `rust-apl/`; never modify the C++ sources.
- Run `cargo fmt && cargo test` before claiming done; verify REPL by piping commands.

---

## Next steps (for the next session)

**Defined functions COMPLETE (see PROGRESS-20260823.md)** — recursive FAC works.

Per `REWRITE_STRATEGY.md` §7 Phase 6 continuation:

1. **System commands** ()VARS, )CLEAR, )FNS; ⎕-system variables (⎕IO, ⎕CT)
2. **Boxed display** of nested arrays
3. **Take/drop with axis spec**, first-axis take
4. Later: Rayon parallelism, error trapping (⎕EA), :If/:While control structures
