# Progress — 2026-09-01

> **Context-preservation file.** Each chat session must check today's date and
> edit `rust-apl/META-INF/PROGRESS-YYYYMMDD.md`. Read the most recent
> progress file(s) before continuing any work in this repo.

---

## Session 106 (2026-09-01) — Control blocks, display fixes, documentation

| Session | Feature | Tests | Differential |
|---|---|---|---|
| 106 | Control blocks in dfns, negative number display, GTK calculator fix, README rewrite, embedding docs | 746 | 375 |

**Changes:**
- `src/parser.rs` — `Expr::Dfn` now carries `Vec<ControlBlock>`; `parse_dfn_body_expr` scans control markers and builds `ControlBlock`s; `flatten_dfn_body` returns `(stmts, control)`; `dfn_to_function` accepts control parameter; all callers updated
- `src/unparse.rs` — Updated `Expr::Dfn` pattern match for new variant shape
- `src/boxdisplay.rs` — `high_minus()` now produces `−` (U+2212) instead of `¯` (U+00AF) for visibility in GTK monospace font
- `src/format.rs` — Dyadic format produces `−` for negatives
- `src/main.rs` — REPL display produces `−` for negatives
- `src/tokenizer.rs` — Accepts both `¯` and `−` as input for negative numbers
- `src/plugins/gtk.rs` — GTK init error handling; display text tracked locally in `RefCell` to avoid GTK buffer stale reads
- `examples/calc-demo.apl` — Event loop with `:If`/`:While`/`:EndIf`/`:EndWhile`
- `README.md` — Rewritten in marketing style, references `docs/` directory
- `docs/embedding.md` — New guide for using `apl` crate in other Rust projects
- `docs/README.md` — Updated table of contents with embedding link
- Removed debug files (`examples/dbg.rs`, `examples/dbgB.rs`, `examples/tok_test.rs`)
- Moved `PROMPT.md` to `META-INF/`

**REPL verified:**
```
∇R ← FACT N
  :If N ≤ 0
    R ← 1
    →0
  :EndIf
  R ← N × FACT N-1
∇
FACT 5     → 120
FACT 10    → 3628800

∇R ← TESTWHILE N
  R ← 0
  :While N > 0
    R ← R + N
    N ← N - 1
  :EndWhile
∇
TESTWHILE 10  → 55

∇R ← TESTNESTED N
  R ← 0
  :While N > 0
    :If N > 5
      R ← R + N
    :Else
      R ← R - N
    :EndIf
    N ← N - 1
  :EndWhile
∇
TESTNESTED 10  → 25

∇R ← TESTLEAVE N
  R ← 0
  :While N > 0
    :If N = 3
      :Leave
    :EndIf
    R ← R + N
    N ← N - 1
  :EndWhile
∇
TESTLEAVE 5  → 9

{⍵+1} 5   → 6
R ← {⍵+1} 5 ⋄ R  → 6
5+2×6-9   → −1
```

**GTK calculator verified:**
- Compute button works repeatedly
- Results accumulate in display
- Negative numbers display correctly with `−`
- Window close event handled

**Current state:**
- 746 unit tests passing
- 375/375 differential cases agree with GNU APL
- Control blocks and branches work inside both named dfns (`∇...∇`) and inline dfns (`{...}`)
- GTK calculator fully functional
- Release binary: 4.6MB, self-contained, no bundled deps needed

---

## Module Summary

| Module | Status | Notes |
|---|---|---|
| 1. ASCII Plot Wire-up | ✅ Complete | `⎕APLOT` renders bar charts |
| 2. Fetcher Function Pointer | ✅ Complete | `CellStorage::Fetched` with `Arc<dyn Fn>` + `OnceLock` |
| 3. ⎕PLOT Properties | ✅ Complete | Dyadic config, auto_open (default false) |
| 4. Python Pipe | ✅ Complete | Shell-out via `python3 -c`, JSON parsing |
| 5. GTK GUI | ✅ Complete | Thread-local GTK4 window with mpsc channel |
| 6. Bidirectional Python | ✅ Complete | ⍎ execute, → branch, recursive dfns |
| 7. Plot Window Mgmt | ✅ Complete | `auto_open` config property |
| 8. Dfn Control Blocks | ✅ Complete | `:If`/`:While`/`:Repeat` and `→label` inside dfns |
| 9. Negative Number Display | ✅ Complete | `−` (U+2212) for visibility, both `¯`/`−` accepted as input |
| 10. GTK Calculator Fix | ✅ Complete | Local display text tracking, repeated Compute works |
| 11. Documentation | ✅ Complete | README marketing rewrite, embedding guide, docs TOC |

---

## Remaining Work

### GTK Calculator (Phase G4)
- ~~Window lifecycle (close/reopen)~~ ✅ Window close event handled
- Plot/Table tabs in GTK notebook — Plot button wired to ⎕PLOT
- ~~Integration with ⎕PLOT for inline graphics~~ ✅ Plot button evaluates and plots

### Optional Enhancements
- ~~`:Leave` inside dfns~~ ✅ Verified working
- ~~`:Repeat`/`:Until` loops~~ ✅ Verified working
- ~~Nested control blocks~~ ✅ Verified working

### Future: Binary Output
- Approach 1: Freeze (embed APL source in Rust wrapper) — days
- Approach 2: APL → C via libapl — weeks
- Approach 3: APL → Rust → binary — weeks-months
- Approach 4: LLVM IR backend — months
