# Progress — 2026-09-01

> **Context-preservation file.** Each chat session must check today's date and
> edit `rust-apl/META-INF/PROGRESS-YYYYMMDD.md`. Read the most recent
> progress file(s) before continuing any work in this repo.

---

## Session 106 (2026-09-01) — Control blocks inside dfns

| Session | Feature | Tests | Differential |
|---|---|---|---|
| 106 | Control blocks (`:If`/`:While`/`:Repeat`) and branches (`→label`) inside dfns | 746 | 375 |

**Changes:**
- `src/parser.rs` — `Expr::Dfn` now carries `Vec<ControlBlock>`; `parse_dfn_body_expr` scans control markers and builds `ControlBlock`s; `flatten_dfn_body` returns `(stmts, control)`; `dfn_to_function` accepts control parameter; all callers updated
- `src/unparse.rs` — Updated `Expr::Dfn` pattern match for new variant shape
- `src/plugins/gtk.rs` — GTK init error handling (don't panic on failure)
- `examples/calc-demo.apl` — Event loop with `:If`/`:While`/`:EndIf`/`:EndWhile`

**REPL verified:**
```
∇R ← FACT N
  :If N ≤ 0
    R ← 1
    →0
  :EndIf
  R ← N × FACT N-1
∇
FACT 5     → 120
FACT 10    → 3628800

∇R ← TESTWHILE N
  R ← 0
  :While N > 0
    R ← R + N
    N ← N - 1
  :EndWhile
∇
TESTWHILE 10  → 55

{⍵+1} 5   → 6
R ← {⍵+1} 5 ⋄ R  → 6
```

**Current state:**
- 746 unit tests passing
- 375/375 differential cases agree with GNU APL
- Control blocks and branches work inside both named dfns (`∇...∇`) and inline dfns (`{...}`)

---

## Module Summary

| Module | Status | Notes |
|---|---|---|
| 1. ASCII Plot Wire-up | ✅ Complete | `⎕APLOT` renders bar charts |
| 2. Fetcher Function Pointer | ✅ Complete | `CellStorage::Fetched` with `Arc<dyn Fn>` + `OnceLock` |
| 3. ⎕PLOT Properties | ✅ Complete | Dyadic config, auto_open (default false) |
| 4. Python Pipe | ✅ Complete | Shell-out via `python3 -c`, JSON parsing |
| 5. GTK GUI | ✅ Complete | Thread-local GTK4 window with mpsc channel |
| 6. Bidirectional Python | ✅ Complete | ⍎ execute, → branch, recursive dfns |
| 7. Plot Window Mgmt | ✅ Complete | `auto_open` config property |
| 8. Dfn Control Blocks | ✅ Complete | `:If`/`:While`/`:Repeat` and `→label` inside dfns |

---

## Remaining Work

### GTK Calculator (Phase G4)
- Window lifecycle (close/reopen)
- Plot/Table tabs in GTK notebook
- Integration with ⎕PLOT for inline graphics

### Optional Enhancements
- `:Leave` inside dfns (currently parsed but no-op)
- `:Repeat`/`:Until` loops (control block built but not yet tested in dfns)
- Nested control blocks (e.g., `:If` inside `:While`)
