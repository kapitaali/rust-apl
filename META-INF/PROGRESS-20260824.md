# Progress — 2026-08-24

> **Context-preservation file.** Each chat session must check today's date and
> edit `rust-apl/META-INF/PROGRESS-YYYYMMDD.md` (create it in that directory
> if missing). Read the most recent progress file(s) before continuing any
> work in this repo.

---

## Session 17 (2026-08-24, later) — GitHub + function persistence + ∧∨ + ⎕PP DONE

Continued Session 16's queue. Repo now lives at github.com/kapitaali/rust-apl.

### 0. Directory renamed apl-rust → rust-apl; GitHub wired up

1. `~/Apps/apl-2.0/rust-apl` is now the repo root (was `apl-rust/`). All
   META-INF docs updated to the new paths.
2. git init; `.gitignore` (target/, *.aplws); README rewritten (200-test era).
3. Remote origin = https://github.com/kapitaali/rust-apl — branch is **main**
   (initial push landed as master; renamed local branch to main,
   force-pushed over the placeholder remote main, deleted remote master).
   Identity: theb <oletus@protonmail.com>. PAT stored via credential.helper
   store. NOTE: keep the PAT out of the remote URL (it was briefly embedded;
   cleaned up).

### 1. Raw source retention → function persistence in )SAVE — DONE

1. `DefinedFunction.source: Vec<String>`: compacted line list (blank lines
   dropped, control markers kept). CRITICAL INVARIANT: body slots,
   leave_lines, control-block start/end, and source ALL index this same
   compacted list. define_function now compacts FIRST, then parses and
   scans control blocks from the compacted list (previously scan ran on the
   raw list — latent mismatch if blank lines preceded markers).
2. workspace.rs save() writes `FN <header>` + `B <line>` records (header
   reconstructed as [result←]NAME [left] [right]); load() parses with an
   index-based walk (V self-contained, FN owns following B-lines) — no
   iterator push-back needed, consecutive functions work.
3. Tests: FINDSTOP (:While+:Leave) round-trips and still executes; two
   functions + a var survive save/clear/load.

### 2. Boolean prims ∧ ∨ (+ classic inner products) — DONE

1. `cell::bif_and`: boolean AND; near-zero short-circuit → Int 0; 1∧1 → 1;
   int generalization = LCM (checked_mul overflow → DOMAIN ERROR).
   `cell::bif_or`: boolean OR; int generalization = GCD. Floats/complex/
   chars → DOMAIN ERROR (simplified vs GNU APL's real gcd/lcm paths).
   Mirrors NumericCell::bif_and / bif_or.
2. Prim::And/Prim::Or registered in tokenizer PRIM_SYMBOLS ("∧","∨"),
   from_symbol, eval_dyadic. Dyadic-only (no monadic form, matching GNU APL).
3. **ROOT-CAUSE FIX for inner products**: operators.rs apply_prim handled
   only 8 primitives and returned SyntaxError for everything else — so
   reduce(And,...) inside f.g failed. Extended apply_prim to all comparison
   prims (Equal/NotEqual/Less/LessEq/Greater/GreaterEq via new cell-level
   bif_equal/bif_less/... helpers mirroring functions.rs closures) plus
   And/Or.
4. Verified: `1 0 1∧.=1 1 1 → 0`, `1 0 1∨.≠0 0 0 → 1`, `2∨4 → 2` (gcd),
   scalar ∧/∨.

### 3. ⎕PP honored in float display — DONE

1. `sysvars::get_pp()`: reads ⎕PP (Int or Float), default 10, values
   outside 1..=20 fall back to 10.
2. boxdisplay: `render_with_pp(v, pp)` threads pp through every arm;
   plain_cell formats floats with `format!("{:.*}", pp, f)` then trims
   trailing zeros/dot. `render()` delegates with pp=10 for library callers.
3. main.rs: format_cell/format_value/format_nested take pp; REPL reads
   ⎕PP before each display, uses render_with_pp for boxed output.
4. Tests: get_pp default/writable/out-of-range; render_with_pp("÷3",4) =
   "0.3333" while render() keeps "0.3333333333".

### Final state

- **204 tests passing**, zero clippy warnings (--all-targets), release build verified
- Test count trajectory: 180 → 204 across today's sessions
- All work committed and pushed to origin/main (commits 496fadf, 87a6198, 13c374c)
- REPL verified: ⎕PP←3 shrinks ÷3 to 0.333; ∧∨ scalars + inner products;
  function+var persistence survives )SAVE/)CLEAR/)LOAD

### Next steps (next session)

1. **dfns `{}`** — biggest remaining language gap (lexer braces, dfn
   environment/self-reference ⍺ ⍵ ∇)
2. **Rank≥2 arrays in workspace save** (serialize dims + ravel generically)
3. **Rayon for reduce/scan** (fold direction must be preserved — chunked
   sequential folds per thread)
4. Later: full ⎕-system (⎕WA ⎕TS ⎕DL...), complex arithmetic, libapl
   embedding, first-axis reduce/scan for defined ops
