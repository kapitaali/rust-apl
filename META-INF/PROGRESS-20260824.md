# Progress — 2026-08-24

> **Context-preservation file.** Each chat session must check today's date and
> edit `rust-apl/META-INF/PROGRESS-YYYYMMDD.md` (create it in that directory
> if missing). Read the most recent progress file(s) before continuing any
> work in this repo.

---

## Session 16 (2026-08-24) — inner product + rayon + :Leave + )SAVE/)LOAD DONE

Resumed from PROGRESS-20260823.md Session 15 "Next steps". Completed all
queued items plus the ⎕IO polish item.

### 1. Inner product `A f.g B` — DONE

1. **`src/inner.rs`**: `inner_product(a, f, g, b)` — contraction axis is
   A's LAST axis and B's FIRST axis (LENGTH_ERROR on mismatch). Scalars
   promote to 1-element axes so `x+.×M` works. Each result cell = reduce-f
   over elementwise-g pairings (reuses operators::reduce, so empty axis →
   f's identity). Result shape = A's leading dims ++ B's trailing dims.
2. Tokenizer: new fallthrough arm for `.` after a Prim token — matches
   `.`+PRIM_SYMBOLS, merges into `Tok::InnerDot(f, g)`. LESSON: this arm
   lives inside tokenize()'s `_ =>` match; use `continue 'outer`, NOT
   `return Ok(())`.
3. Parser: `Expr::InnerProduct(f, g, lhs, rhs)` arm in parse_simple placed
   right after the OuterDot arm (before plain dyadic).
4. Tests: dot product 140, matrix·vector, matrix·matrix, `⌈.+` (=9),
   LENGTH_ERROR, parser-level `M+.×5 6 7 → 20 74`. NOTES: dyadic ⌈/⌊ always
   return Float cells (pre-existing cell.rs behavior). Boolean ∧.= / ∨.≠
   impossible yet — no ∧∨ prims. Test gotcha: eval_line returns None for
   pure assignment lines — call env.eval_line().unwrap() directly, not
   eval_one(), for those.

### 2. Rayon parallelism for elementwise ops — DONE (via Rc→Arc swap)

1. **Architectural prerequisite solved**: `Cell::Pointer` held
   `std::rc::Rc<ValueInner>`, blocking Send/Sync. Mechanical swap of ALL
   `Rc` → `std::sync::Arc` across 12 files (~28 sites) incl. `Arc::make_mut`
   for COW. Zero semantic changes; all existing tests passed unchanged.
   pick.rs test helper renamed Rc_value → rc_value for clippy.
2. **rayon = "1.10"** added to Cargo.toml.
3. **functions.rs**: `PARALLEL_THRESHOLD = 4096`. `map_cells` (monadic)
   parallelizes above threshold via par_iter().map().collect::<Result>();
   `elementwise` (dyadic) parallelizes only when ac==bc (same-shape; the
   scalar-extension modulo path stays sequential). Closures now require
   `+ Sync + Send`.
4. **`src/parallel_tests.rs`** (`#[cfg(test)]` module): pins results at
   5000 elements vs below-threshold correctness.

### 3. :Leave control structure — DONE

1. `DefinedFunction.leave_lines: Vec<usize>` records :Leave body slots
   (`is_control_marker` recognizes it; slots stay Expr::Num(0.0) no-ops).
2. Mechanism: `run_lines` checks each pc against leave_lines; on hit pushes
   `LEAVE_SENTINEL = i64::MIN` onto branch_stack (a value no real branch
   target can take) and returns Ok. The While/Repeat loop arms call
   `consume_leave()` after each body pass — true → break. Repeat checks
   BEFORE :Until (leave short-circuits the until check).
3. Tests: FINDSTOP exits an infinite `:While 1` via :Leave (FINDSTOP 3=6);
   LEAVECOUNT terminates an :Until-less :Repeat via :Leave (=N).

### 4. Workspace save/load )SAVE / )LOAD — DONE

1. **`src/workspace.rs`**: line format `APLWS1` header + `V <name> <kind>
   <payload>` records. Kinds: I/F/C scalars, VI int-vector, VC char-vector
   (codepoints as decimal u32s, comma-separated). Unsupported shapes
   (matrices, nested) REFUSE to save with a clear error — never truncate
   silently. )LOAD wipes current workspace first then re-seeds sysvars
   (GNU APL semantics).
2. Function persistence NOT implemented: DefinedFunction stores parsed
   Exprs without source lines, so save() errors if any function exists
   rather than writing an incomplete workspace. (Future: retain raw source
   lines in DefinedFunction.)
3. syscmd dispatches )SAVE name / )LOAD name (missing arg → usage line;
   errors print as ERROR: ...). GOTCHA: Cell::Char holds u32 (Unicode type
   alias), not char — serialize in u32 space.
4. Tests: roundtrip (scalar int/float, int vec, char vec), load-wipes-
   existing, unsupported-value error, missing-file error.

### 5. ⎕IO polish audit — RESOLVED AS DOCUMENTED CONTRACT

Audited sort.rs grade callers: grade_up/grade_down (io=0 delegates) are
UNREACHABLE from the interpreter — parser.rs Monadic arm intercepts
⍋/⍒ before Prim::eval_monadic and calls grade_io(get_io()); dyadic ⍳ same
(index_of_io intercept). The io=0 variants are library-only helpers by
design; doc comments now state this contract explicitly, locked with a
test asserting `⎕IO←1` + `⍋B` yields 1-based results through eval_line.

### Final state

- **198 tests passing**, zero clippy warnings (--all-targets), release build verified
- Test count trajectory: 180 → 198
- REPL verified end-to-end: inner product (140 / 20 74), save-clear-load
  roundtrip (X=42, +/V=10), ∇+:While+:Leave function (FINDSTOP 4=10)

### Next steps (next session)

1. **Retain raw source lines in DefinedFunction** → enable function
   persistence in workspace files (header + body already have a file
   format waiting: FN/B records parsed by workspace.rs load()).
2. **Boolean prims ∧ ∨** (and/or) — unblocks classic inner products ∧.= ∨.≠
3. **⎕PP actually applied to float formatting** (display currently
   hardcodes 10 decimals in main.rs/boxdisplay.rs)
4. Later: rank≥2 arrays in workspace save, Rayon for reduce/scan (fold
   direction must be preserved), dfns `{}`
